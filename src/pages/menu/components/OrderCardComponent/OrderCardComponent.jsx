import React from 'react';
import styles from './OrderCardComponent.module.scss';

const OrderCardComponent = () => (
  <div className={styles.orderCardContainer}>
    <h1>OrderCardComponent</h1>
  </div>
);

export default OrderCardComponent;
